   ---
    DEMO7 — End-to-end verification (paste this whole block to orchestrator)

    [DEMO7-START]

    You are orchestrating a comprehensive end-to-end verification dance for Cartograph
    covering Phase 4 + 4.1 + 4.2 (mutation/proxy lifecycle) + Phase 5 (admin UI tweaks +
    audit + insights + persistence toggle) + Phase 7 (terminal acks, pre-merge handoff,
    self-loops, catalogs first-class) + Phase 7.4.2 (flows reference catalogs first-class +
    catalog cascade in mutation + admin UI catalog endpoints) + Phase 7.4.4 + 7.4.5
    (DEMO7-round-1 bug fixes — self-loop guards dropped, transfer_catalog_ids exposed,
    vector_search lean projection, get_my_catalogs dedup post-merge, get_action_items
    _summary uniform-int, SME prompt cleanup). This is a structured 16-phase test — each
    phase verifies a specific feature surface.

    == REPORT-BACK CONVENTIONS ==

    Tag every status / observation message with one of:
      [DEMO7-PHASE-N]    section header (N = 1..16)
      [DEMO7-OK]         a verification passed
      [DEMO7-FAIL]       a verification failed (note expected vs actual)
      [DEMO7-BUG]        unexpected behavior worth flagging
      [DEMO7-NOTE]       informational observation
      [DEMO7-RESULT]     final scorecard at the end

    The user will grep these prefixes and paste back to Claude for analysis.
    DO NOT use these prefixes for anything else.

    == TOPOLOGY ==

    Spawn 4 SMEs covering 4 components:
      sme-auth-1     → auth-svc (handles HTTP /verify endpoint)
      sme-auth-2     → auth-svc-v2 (DUPLICATE of auth-svc — should merge)
      sme-monolith   → monolith-x (contains BOTH "payments" and "feeds" — should split)
      sme-cron       → cron-rebalance (CRON job, self-triggers daily — exercises self-loop)

    You may use bulk_spawn_smes against a synthetic plane='github' resource set, or
    create_agent individually. Either is fine.

    == PHASE 1 — Setup + materialisation ==

    Spawn the 4 SMEs. Each materialises its component:
      - upsert_component with appropriate canonical_name, display_name,
        component_type='application' (or 'cron' for sme-cron)
      - upsert_attribution exhaustively (hostnames, deploy configs, etc.)

    REPORT [DEMO7-PHASE-1] when all 4 SMEs have materialised.
    REPORT [DEMO7-OK] for each component created.
    List the 4 component_ids.

    == PHASE 2 — Catalogs first-class (Phase 7.4) ==

    Each SME declares catalogs using `upsert_catalog` (noun-form kind, NOT the
    deprecated upsert_edge_catalog):

      sme-auth-1:    upsert_catalog(component_id, kind='endpoint',
                                    identifier='POST /verify')
      sme-auth-1:    upsert_catalog(component_id, kind='endpoint',
                                    identifier='GET /token')
      sme-monolith:  upsert_catalog(component_id, kind='endpoint',
                                    identifier='POST /payments/charge')
      sme-monolith:  upsert_catalog(component_id, kind='endpoint',
                                    identifier='GET /feeds/scores')
      sme-cron:      upsert_catalog(component_id, kind='trigger_target',
                                    identifier='daily-rebalance')

    Then verify:
      - sme-auth-1 calls get_my_catalogs() → expect 2 rows (POST /verify + GET /token)
      - sme-cron   calls get_my_catalogs() → expect 1 row (daily-rebalance)

    REPORT [DEMO7-PHASE-2] section header.
    REPORT [DEMO7-OK] / [DEMO7-FAIL] for each catalog declaration + each get_my_catalogs result.

    == PHASE 3 — Bound calls + unmatched-caller triage (Phase 7.4) ==

    sme-auth-1 creates an outbound bound call to sme-monolith:
      upsert_edge_outbound(from=auth-svc_id, to=monolith-x_id,
                           edge_type='calls', identifier='POST /payments/charge')
      → matches monolith's catalog. Verify by calling
        get_my_catalog_callers from sme-monolith and confirm sme-auth-1 appears.

    sme-auth-1 creates a SECOND outbound bound call with a non-matching identifier:
      upsert_edge_outbound(from=auth-svc_id, to=monolith-x_id,
                           edge_type='calls', identifier='POST /unknown-endpoint')
      → does NOT match any monolith catalog. Verify by calling
        get_unmatched_callers from sme-monolith and confirm '/unknown-endpoint'
        appears.

    sme-monolith triages: raise a clarification to sme-auth-1 asking "did you mean
    to call /unknown-endpoint? I don't expose it." (sme-auth-1 should respond with
    QC saying it was a typo / will fix.)

    REPORT [DEMO7-PHASE-3] section header.
    REPORT [DEMO7-OK] / [DEMO7-FAIL] for each verification.

    == PHASE 4 — Self-loop edge (Phase 7.3 + 7.4.4) ==

    sme-cron exercises self-loops on ALL THREE write paths (Phase 7.4.4
    dropped the Python guards that DEMO7-round-1 blocked on):

    4a. upsert_edge_outbound with from = to:
        upsert_edge_outbound(from=cron_component_id, to=cron_component_id,
                             edge_type='triggers', identifier='daily-rebalance')

    4b. create_edge (legacy shim) with source = target:
        create_edge({source_id=cron_component_id, target_id=cron_component_id,
                     edge_type='calls', identifier='self-recurse'})

    4c. bind_edge resolving a dangling outgoing back to self:
        - First create dangling: upsert_edge_outbound(from=cron, to=NULL,
          edge_type='calls', identifier='lazy-self')
        - Then resolve: bind_edge(dangling_id, to_component_id=cron_component_id)

    Pre-7.4.4 ALL THREE blocked with verbatim "must differ (no self-loops)" /
    "Cannot bind to self (no self-loops)" — that's the exact bug DEMO7-round-1
    surfaced as [DEMO7-BUG Phase-4 self-loop]. Phase 7.4.4 dropped the
    application-layer Python guards. All three should now succeed.

    REPORT [DEMO7-PHASE-4] section header.
    REPORT [DEMO7-OK] for each of 4a / 4b / 4c (3 separate lines).
    REPORT [DEMO7-FAIL] with verbatim error if any path still raises.

    == PHASE 5 — Merge nomination + auto-escalation + resolver review ==

    sme-auth-1 nominates a merge with sme-auth-2 (canonical_name + attributions
    overlap). Use confidence ≥ 0.85 in the nomination message so it auto-escalates.

    sme-auth-2 responds with confidence ≥ 0.85 (matching).

    The auto_transitions scanner should escalate to R (resolver review) once both
    scores are ≥ 0.85.

    Resolver reviews: read the consolidation thread, verify evidence, approve with
    new_status='M' and mutation_assigned_to=sme-auth-1.

    REPORT [DEMO7-PHASE-5] section header.
    REPORT [DEMO7-OK] for: nomination, response, auto-escalation visible (status='R'),
                           resolver review (status='M', mutation_assigned_to set).

    For Phase 5.6 verification: read the consolidation thread and confirm each
    state-change message has metadata.confidence_at_send = {a, b, r}. Report
    [DEMO7-OK] if every response has the snapshot, [DEMO7-FAIL] otherwise.

    == PHASE 6 — Pre-merge handoff (Phase 7.2) + absorb (Phase 4) + terminal ack
                (Phase 7.1) ==

    This is the big one — exercises three Phase 7 features in sequence.

      6a. PRE-MERGE HANDOFF (7.2):
          sme-auth-1 (mutation POC) MUST raise a clarification to sme-auth-2
          BEFORE absorb_agent:
            create_clarification(asker=sme-auth-1, responder=sme-auth-2,
              question_message="Pre-merge handoff: I'm about to absorb you. Brief me
              on configs / runtime nuances / monitoring quirks NOT in your
              component_doc_md / source_slice / attributions / edges / flows.
              Respond with QC.")
          sme-auth-2 responds with QC and a one-paragraph brief mentioning at least
          one nuance (e.g., "deploy needs special env var AUTH_SECRET_KEY; on-call
          rotation in pagerduty schedule SCHED-123").
          sme-auth-1 reads the response and updates its own component_doc_md via
          upsert_component to capture the brief.

          REPORT [DEMO7-OK] if handoff happened in this order; [DEMO7-FAIL] if
          sme-auth-1 absorbed without the clarification.

      6b. ABSORB (4.1 cascade + 7.4.2 catalog cascade):
          sme-auth-1 calls absorb_agent(consolidation_id, target=sme-auth-2,
                                        deactivation_reason='merged',
                                        deactivation_notes='auth-svc-v2 folded in')
          with default cascade flags.

          Verify:
            - sme-auth-2.status = 'decommissioned'
            - sme-auth-2.merged_into_agent_id = sme-auth-1
            - sme-auth-2's attributions transferred to sme-auth-1's component
            - sme-auth-2's component status = 'decommissioned'
            - The absorb_agent response now carries `cascade.catalogs` >= 0
              (Phase 7.4.2). For sme-auth-2's catalogs from Phase 2:
              POST /verify, POST /refresh — sme-auth-1 also declared
              POST /verify, so the (kind, identifier) collision drops
              sme-auth-2's POST /verify catalog AND any flows referencing
              it (cascade-delete via FK). POST /refresh re-points to
              sme-auth-1's component (no collision).
            - Spot-check: sme-auth-1 calls get_my_catalogs() and verifies
              both POST /verify and POST /refresh are now under its
              component. POST /verify caller_count should reflect bound
              edges (kyc-svc still binds to it).

      6c. EXECUTE + COMPLETE (Phase 4):
          sme-auth-1 calls execute_mutation → status M → MD.
          Resolver calls complete_consolidation → status MD → D.

      6d. TERMINAL ACK (7.1) — the new behaviour:
          After complete_consolidation, the consolidation status is D. Both
          sme-auth-1 (still active) AND resolver should now have this consolidation
          in their `terminal_pending_ack` bucket. Verify by:
            - sme-auth-1 calls get_action_items_summary → terminal_pending_ack > 0
            - resolver calls get_action_items_summary → terminal_pending_ack > 0
            - Each calls ack_terminal('consolidation', consolidation_id).
            - Re-call get_action_items_summary → terminal_pending_ack drops to 0.

          Verify decommissioned sme-auth-2 was AUTO-ACKED via absorb_agent (i.e.,
          a row exists in terminal_acks for sme-auth-2 even though it never
          explicitly acked).

          REPORT [DEMO7-OK] for each verification step.

    REPORT [DEMO7-PHASE-6] section header before any 6X work begins.

    == PHASE 7 — Split + transfers (Phase 4.1) ==

    sme-monolith nominates a split (carve out "payments" into a new component).
    Status should land directly at R (no B2 — Phase 4 split rule).

    Resolver reviews → M with mutation_assigned_to=sme-monolith.

    sme-monolith calls spawn_child_agent with:
      - child_id="sme-payments-split"
      - component_data with appropriate canonical_name, display_name
      - child_source_slice (the carved-out subset of monolith's slice)
      - briefing: "You are the payments-svc carved from monolith-x. Your scope: ..."
      - transfer_catalog_ids: [the catalog id for POST /payments/charge — Phase 7.4.2]
      - transfer_edge_ids: [any bound edges originating from the payments side]
      - transfer_flow_ids: [flow rows whose component_id == monolith AND
        whose incoming_catalog_id is in transfer_catalog_ids]
      - transfer_attribution_ids: [any attributions belonging to the payments part]

    Verify:
      - sme-payments-split exists with status='idle'
      - The new component has split_from_component_id pointing to monolith
      - The transferred catalogs / edges / attributions now belong to
        sme-payments-split (the response carries
        `transferred_catalogs`, `transferred_edges`,
        `transferred_attributions` counters — Phase 7.4.2)
      - The transferred flow's `incoming_catalog_id` still points at the
        same catalog row (which is now under the child) — flow integrity
        preserved across the split
      - A welcome [split-welcome] task exists for sme-payments-split

    sme-monolith calls execute_mutation → MD. Resolver calls complete_consolidation → D.

    Both sme-monolith and resolver get terminal_pending_ack for THIS consolidation
    too. Each acks via ack_terminal.

    REPORT [DEMO7-PHASE-7] section header.
    REPORT [DEMO7-OK] for each verification + the ack flow.

    == PHASE 8 — Proxy inheritance (Phase 4) ==

    Admin sends a chat message to sme-auth-2 (decommissioned).

    Wait a few seconds. Then have sme-auth-1 query get_my_proxy_items() — verify
    the chat from admin to sme-auth-2 surfaces under sme-auth-1's proxied items
    (grouped by proxy_agent_id=sme-auth-2, depth=1).

    sme-auth-1 responds via act_on_proxy_item:
      act_on_proxy_item(survivor=sme-auth-1, item_type='chat', item_id=<chat_comm_id>,
                        action='send',
                        payload={proxy_agent_id='sme-auth-2', to_agent_id='admin',
                                 message='Replying on behalf of sme-auth-2 (now decommissioned).'})

    Verify:
      - A new chat row exists from sme-auth-2 (the proxy) to admin
      - A proxy_audit row exists for this act
      - Admin sees the response in their chat history

    REPORT [DEMO7-PHASE-8] section header.
    REPORT [DEMO7-OK] / [DEMO7-FAIL] for each step.

    == PHASE 9 — Insights (Phase 5.9) ==

    Each of sme-auth-1, sme-monolith, sme-cron calls record_insight at least once
    during the demo, capturing real observations:
      record_insight(agent_id, kind, target, body, evidence?)
    where kind ∈ {prompt_gap, tactic_win, tool_gap, doc_confusing, workflow_friction}.

    For example:
      sme-auth-1: record_insight(kind='tactic_win', target='absorb_agent',
                                 body='Pre-merge handoff via clarification works
                                 well — captured a deploy nuance that would have
                                 been lost otherwise.')

    REPORT [DEMO7-PHASE-9] section header.
    REPORT [DEMO7-OK] with a count of insights recorded by SMEs.

    == PHASE 10 — Persistence toggle + confidence-at-send round-trip ==

    10a. PERSISTENCE TOGGLE (Phase 5.7):
         Send a broadcast yourself with persistent=False to all SMEs.
         Then call update_broadcast_persistence(comm_id, persistent=True).
         Verify the row's is_persistent flipped via direct read or via /api/communications.

    10b. CONFIDENCE-AT-SEND (Phase 5.6):
         Already verified in Phase 5. Re-state the result here for completeness.

    REPORT [DEMO7-PHASE-10] section header.
    REPORT [DEMO7-OK] / [DEMO7-FAIL].

    == PHASE 11 — MCP audit (Phase 5.10) ==

    You have made many tool calls during this demo. The mcp_audit table should have
    rows for every one. Spot-check by issuing a count query (you cannot directly
    SELECT from the table via MCP — flag this if you don't have visibility, but the
    admin UI exposes /api/mcp_audit?agent_id=...&limit=10 if you have HTTP access
    via bash/curl).

    Use bash + curl if available:
      curl -s 'http://localhost:8200/api/mcp_audit?agent_id=sme-auth-1&limit=5'

    Verify the returned entries include recent tool names like upsert_catalog,
    absorb_agent, ack_terminal.

    REPORT [DEMO7-PHASE-11] section header.
    REPORT [DEMO7-OK] / [DEMO7-FAIL] / [DEMO7-NOTE] (if no HTTP access).

    == PHASE 12 — Flows reference catalogs first-class (Phase 7.4.2) ==

    The most important new mechanic: a flow's incoming is canonically a
    catalog row (NOT an edge id). This phase exercises declaration,
    fan-out, both read tools, the new contract on get_flow_inverse,
    the negative-path validation, AND seeds enough flows across the
    topology so the admin can validate the 3D graph viz interactively
    (LOS BFS, junction conflation, hover zones).

    ★ MANDATORY VIZ-SEEDING STEP — every SME with at least one catalog
    AND at least one outgoing edge MUST declare ≥2 flows. The admin
    will click edges in the 3D graph after the demo and watch LOS
    chains light up — this only works if real flow rows exist.

    Per-SME flow targets (orchestrator: assign these in each SME's
    next-wake task brief if not done autonomously):

      sme-auth-1 (after merge — owns POST /verify + POST /refresh +
        absorbed POST /login from sme-auth-2):
          • flow: catalog(POST /verify)  → outgoing edge of your choice
            (e.g. a bound edge auth-svc → feeds-db "SELECT user_token")
          • flow: catalog(POST /refresh) → another outgoing
          → results in 2 visible LOS chain origins on auth-svc

      sme-monolith (before split — owns POST /payments/charge +
        GET /feeds/scores):
          • flow: catalog(POST /payments/charge) → bound outgoing
            (monolith → feeds-db "INSERT ledger")
          • flow: catalog(POST /payments/charge) → second outgoing
            (monolith → notify "payment.completed")  [fan-out demo]
          • flow: catalog(GET /feeds/scores) → bound outgoing
            (monolith → feeds-cache "GET live:scores")
          → results in fan-out junction visible on monolith

      sme-payments-split (post-split, owns the carved POST
        /payments/charge):
          • re-declare 1 flow: catalog(POST /payments/charge) →
            outgoing edge (payments-split → feeds-db "INSERT ledger"
            — re-create if needed)
          → confirms split-side flows render correctly post-mutation

      sme-cron (owns trigger_target daily-rebalance + the self-loop
        edge from Phase 4):
          • flow: catalog(daily-rebalance) → self-loop edge
          → demonstrates self-loop participating in flow rendering

    Total target: ≥6 flow rows across the topology. The admin will
    confirm /api/graph returns flows.length >= 6 AND can click any
    bound edge to watch the LOS chain animate.

    REPORT [DEMO7-NOTE] with the per-SME flow count after PHASE-12
    completes so the admin can spot under-seeding.

    12a. DECLARE A FLOW (single):
         sme-monolith owns a catalog "POST /payments/charge" (declared
         in Phase 2) and a bound outgoing edge to feeds-db at
         "INSERT ledger" (declared in Phase 3 or freshly here via
         upsert_edge_outbound). Wire them:
           upsert_flow(
             agent_id=sme-monolith,
             component_id=monolith_component_id,
             incoming_catalog_id=<POST /payments/charge catalog id>,
             outgoing_edge_id=<INSERT ledger edge id>,
             metadata={"src": "code"}, confidence=0.9,
           )
         Verify the returned row has incoming_catalog_id set (NOT
         incoming_edge_id — that column does not exist anymore).

    12b. FAN-OUT (one catalog → multiple outgoings):
         Add a SECOND outgoing from monolith — e.g. a publishes_to
         to a notify topic — and wire a second flow:
           upsert_flow(monolith, monolith_id, charge_catalog_id,
                       <notify edge id>)
         Confirm get_flow(monolith_id, charge_catalog_id) returns
         BOTH outgoings (set-based fan-out).

    12c. INVERSE LOOKUP (contract change verification):
         get_flow_inverse(monolith_id, <INSERT ledger edge id>) should
         return CATALOG ROWS (not edge rows). Verify the returned row
         has a `kind` field (catalog rows do; edge rows don't) and
         that its id matches charge_catalog_id.

    12d. NEGATIVE PATH — foreign-component catalog refused:
         sme-monolith tries to upsert_flow using sme-cron's catalog id
         as incoming_catalog_id (a catalog NOT belonging to monolith's
         component). Should raise ValueError matching
         "does not belong to component".

    12e. CATALOG CASCADE — flow disappears when catalog is deleted:
         Take a synthetic catalog (kind='topic', identifier='test.delete-me')
         on monolith's component, wire one flow on it, then DELETE the
         catalog (orchestrator can use bash + curl + the database, OR
         have sme-monolith do it via SQL through MCP if available, OR
         skip and report [DEMO7-NOTE] if no path). Verify the flow row
         is gone (FK ON DELETE CASCADE).

    REPORT [DEMO7-PHASE-12] section header.
    REPORT [DEMO7-OK] / [DEMO7-FAIL] for each of 12a–12e.

    == PHASE 13 — Vector search on catalogs (Phase 7.4 follow-up) ==

    Catalogs are embedded at write time ("{kind}: {identifier}") and
    surfaced via vector_search.

    sme-auth-1 calls:
      vector_search(agent_id=sme-auth-1, query_text='login verify endpoint',
                    table='catalogs', limit=5)

    Verify:
      - The response shape is {query_embedded: bool, results: [...]}.
      - If query_embedded is True, the results array is non-empty AND
        sme-auth-1's POST /verify catalog appears (or a near-match) in
        the top 5.
      - If query_embedded is False (Ollama unreachable), report
        [DEMO7-NOTE] explaining the embedding pipeline is down — that's
        a graceful-degrade contract, not a failure.

    Then call:
      vector_search(agent_id=sme-monolith, query_text='charge payment',
                    table='catalogs', limit=5)
    Verify monolith's POST /payments/charge appears.

    Cross-check: vector_search with table='edges' for the same queries
    should NOT return catalog-shaped rows anymore (catalogs are not in
    edges post-Phase-7.4). Just bound + dangling rows.

    REPORT [DEMO7-PHASE-13] section header.
    REPORT [DEMO7-OK] / [DEMO7-FAIL] for each query + the cross-check.

    == PHASE 14 — Admin UI catalog endpoints (Phase 7.4.2) ==

    These FE-facing endpoints were silently broken pre-7.4.2 (returning 0 /
    empty for every component). Verify via curl from bash:

    14a. /api/components.catalog_count > 0:
         curl -s 'http://localhost:8200/api/components?q=auth' | \
           python3 -c "import sys,json; d=json.load(sys.stdin); \
             print([(c['canonical_name'], c['edge_count']) for c in d['components']])"
         Verify: sme-auth-1's component has edge_count.catalog >= 2
         (POST /verify + POST /refresh + any from absorb of sme-auth-2).

    14b. /api/component/{id}/drilldown.edges.catalog populated:
         curl -s "http://localhost:8200/api/component/<auth-1-component-id>/drilldown" | \
           python3 -c "import sys,json; d=json.load(sys.stdin); \
             print('catalog rows:', len(d['edges']['catalog'])); \
             [print(' ', c.get('catalog_kind'), c.get('identifier')) for c in d['edges']['catalog']]"
         Verify: catalog rows are non-empty AND each carries
         `catalog_kind` (e.g. 'endpoint') alongside identifier.

    14c. /api/graph flows[].incoming_catalog_id present:
         curl -s 'http://localhost:8200/api/graph' | \
           python3 -c "import sys,json; d=json.load(sys.stdin); \
             print('flows:', len(d['flows'])); \
             [print(' ', list(f.keys())) for f in d['flows'][:1]]"
         Verify: flow rows carry `incoming_catalog_id` (NOT
         `incoming_edge_id` — that field name no longer exists in the
         payload).

    REPORT [DEMO7-PHASE-14] section header.
    REPORT [DEMO7-OK] / [DEMO7-FAIL] for each of 14a–14c.

    == PHASE 15 — Phase 7.4.4 fix verifications (DEMO7-round-1 regressions) ==

    DEMO7-round-1 surfaced 3 holes that I (Claude) missed; all 3 fixed in
    commit ead74bf. Verify the fixes hold:

    15a. spawn_child_agent MCP wrapper exposes transfer_catalog_ids:
         Phase 7 (split) above already uses this param. Confirm by checking
         the spawn_child_agent response carries `transferred_catalogs >= 1`
         when transfer_catalog_ids was passed. Pre-7.4.4 the param was
         silently dropped at the wrapper boundary even though SMEs supplied
         it — sme-payments-split flagged "the parent monolith-x still owns
         the original POST /payments/charge catalog row" because the
         transfer never fired.

         Cross-verify on the data side: after Phase 7 split, query
         get_my_catalogs(sme-payments-split) — the carved POST
         /payments/charge catalog should appear under the CHILD's component,
         not the parent's. If both show it (or only parent shows it),
         [DEMO7-FAIL] — the wrapper plumb still missing.

    15b. Phase 7.3 self-loop guards stay dead:
         This is the same as PHASE 4 above (4a + 4b + 4c). Just confirm
         all three sub-results from PHASE 4 were [DEMO7-OK]. If any was
         [DEMO7-FAIL] with "no self-loops" verbatim, the Python guard
         regressed.

    15c. vector_search lean projection:
         Any agent calls:
           vector_search(agent_id, 'auth verify endpoint', 'components', 5)
         Verify the response shape:
           - Each result row has keys: {id, canonical_name, display_name,
             component_type, status, similarity}
           - Each result row does NOT carry: embedding, component_doc_md,
             source_slice, metadata (these used to inflate responses to
             50–115KB).
           - Total response size is small (rough check: < 5KB for 5 rows).

         Then call vector_search on each of attributions / unresolved /
         edges / catalogs and confirm none of those leak embedding or
         heavy JSONB blobs either.

    REPORT [DEMO7-PHASE-15] section header.
    REPORT [DEMO7-OK] / [DEMO7-FAIL] for each of 15a / 15b / 15c.

    == PHASE 16 — Phase 7.4.5 fix verifications (DEMO7-round-1 pre-existing bugs) ==

    DEMO7-round-1 also surfaced 3 pre-existing bugs (insights cad0edf5,
    d4c923dc, plus the resolver's get_action_items_summary crashes); all
    fixed in commit 9b94805. Verify:

    16a. get_my_catalogs no duplicates post-merge:
         After Phase 6 absorb completes, sme-auth-1 (the merge survivor)
         calls get_my_catalogs(sme-auth-1).
         Verify:
           - Each catalog id appears EXACTLY ONCE in the response.
           - The total row count equals the distinct count.
           - Specifically: POST /verify (sme-auth-1's original) and
             POST /refresh (transferred from sme-auth-2) each appear once.

         Pre-7.4.5 the JOIN-on-RCA cardinality bug caused each catalog to
         appear 2× post-merge (sme-f9bde48a saw 12 rows for 6 distinct
         catalogs in DEMO7-round-1). [DEMO7-FAIL] if any catalog id
         repeats.

    16b. get_action_items_summary uniform-int shape:
         resolver (which has at least one closed consolidation in
         terminal_pending_ack from Phase 6) calls
         get_action_items_summary(resolver_id).

         Verify:
           - Response is a flat dict with keys: consolidations_pending,
             tasks_pending, clarifications_pending, unacked_chats,
             unacked_broadcasts, terminal_pending_ack, **proxied_count**.
           - EVERY value is an int (no list anywhere).
           - The response does NOT crash with "Input should be a valid
             integer [type=int_type, input_value=[], input_type=list]".
           - Specifically: `proxied` field is GONE; `proxied_count` field
             is present.

         Pre-7.4.5 this crashed on every wake. [DEMO7-FAIL] if pydantic
         error returns OR if the response carries a `proxied: list` field.

         Then call get_action_items_detail(resolver_id) and verify the
         rich per-proxy breakdown lives there: response has both `my` and
         `proxied` buckets, and `proxied` is a list of dicts with
         {proxy_agent_id, deactivation_reason, depth, items}.

    16c. SME prompt update — upsert_catalog (not upsert_edge_catalog):
         Spawn ONE fresh SME (e.g. sme-prompt-check). On its first wake,
         it should declare its catalog using `upsert_catalog`. If it uses
         `upsert_edge_catalog` instead, the Phase 7.4.5 prompt cleanup
         didn't reach the materialisation hygiene cycle.

         Cross-verify: ask the SME via send_chat (admin → sme-prompt-check)
         "Which tool should you use to declare a new endpoint catalog
         row?" — the response should mention `upsert_catalog` not the
         deprecated wrapper.

    REPORT [DEMO7-PHASE-16] section header.
    REPORT [DEMO7-OK] / [DEMO7-FAIL] for each of 16a / 16b / 16c.

    == FINAL SCORECARD ==

    Once all 16 phases complete, emit a [DEMO7-RESULT] block with one line per
    phase:
      [DEMO7-RESULT] Phase 1 setup ................. PASS / FAIL — short summary
      [DEMO7-RESULT] Phase 2 catalogs .............. PASS / FAIL — ...
      [DEMO7-RESULT] Phase 3 unmatched callers ..... PASS / FAIL — ...
      [DEMO7-RESULT] Phase 4 self-loop ............. PASS / FAIL — ... (3 sub-results)
      [DEMO7-RESULT] Phase 5 merge nominate ........ PASS / FAIL — ...
      [DEMO7-RESULT] Phase 6 handoff+absorb+ack .... PASS / FAIL — ...
      [DEMO7-RESULT] Phase 7 split+transfers ....... PASS / FAIL — ...
      [DEMO7-RESULT] Phase 8 proxy inheritance ..... PASS / FAIL — ...
      [DEMO7-RESULT] Phase 9 insights .............. PASS / FAIL — ...
      [DEMO7-RESULT] Phase 10 persistence toggle ... PASS / FAIL — ...
      [DEMO7-RESULT] Phase 11 mcp audit ............ PASS / FAIL — ...
      [DEMO7-RESULT] Phase 12 flows-on-catalogs .... PASS / FAIL — ...
      [DEMO7-RESULT] Phase 13 catalog vector_search  PASS / FAIL — ...
      [DEMO7-RESULT] Phase 14 admin UI catalogs .... PASS / FAIL — ...
      [DEMO7-RESULT] Phase 15 7.4.4 fix verify ..... PASS / FAIL — ... (3 sub-results)
      [DEMO7-RESULT] Phase 16 7.4.5 fix verify ..... PASS / FAIL — ... (3 sub-results)

    Plus list every [DEMO7-BUG] line you emitted earlier (if any) at the bottom of
    the scorecard so the user can quickly identify what to investigate.

    == CLEANUP ==

    After the scorecard, do NOT delete data automatically. Wait for the admin to
    issue a cleanup command. Demo data is tagged via metadata where possible.

    == TIMING ==

    This is a long-running multi-agent dance. Trigger manager + agent manager will
    wake the involved SMEs as needed. You may need to delegate work via tasks
    (create_task with description = "Phase X step Y: <action to take>") and then
    WAIT for the sub-task to complete (status WD or TC) before moving to the next
    step. If something stalls >5 minutes on a single step, raise a [DEMO7-BUG]
    with the stall description and continue if possible.

    GO.
    [DEMO7-END-OF-PROMPT]

    ---

fastapi>=0.110
uvicorn[standard]>=0.27
psycopg[binary]>=3.1
psycopg-pool>=3.1
pydantic>=2.5
grpcio>=1.60
protobuf>=4.25
# grpcio-tools is only needed at build time to regenerate the proto stubs
# (see proto_stubs/regen.sh) — kept here for convenience, can be moved to a
# dev-only requirements file later.
grpcio-tools>=1.60

__pycache__/
*.pyc
.venv/
*.db
workspaces/
workspaces.bak*/
.env
.idea/
docs/superpowers/
*.egg-info/

# DB snapshots (pre-demo / pre-real-data backups). Kept locally, not
# tracked — they can grow large (19MB+ already) and embed row data that
# shouldn't hit origin.
snapshots/
*.sql
